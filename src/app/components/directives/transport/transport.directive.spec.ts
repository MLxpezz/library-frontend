import { TransportDirective } from "./transport.directive";


describe('TransportDirective', () => {
  it('should create an instance', () => {
    const directive = new TransportDirective();
    expect(directive).toBeTruthy();
  });
});
