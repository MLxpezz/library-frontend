import { TransportToDirective } from './transport-to.directive';

describe('TransportToDirective', () => {
  it('should create an instance', () => {
    const directive = new TransportToDirective();
    expect(directive).toBeTruthy();
  });
});
